const apiConfig = {
  baseURL: 'https://sua-api.com/api', // Substitua pela URL da sua API
  headers: {
    'Content-Type': 'application/json',
    // Adicione outros cabeçalhos se necessário, como autenticação
  },
  timeout: 5000, // Tempo de espera em milissegundos
};

export default apiConfig;
